# Supabase / PostgreSQL
# For Supabase: use pooler URL for DATABASE_URL, direct for DIRECT_URL
# For local PostgreSQL: use same URL for both
DATABASE_URL="postgresql://user:password@localhost:5432/attendance_db?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/attendance_db?schema=public"

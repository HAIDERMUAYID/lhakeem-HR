# خطة: تقويم وحدة البصمة

## الهدف
صفحة **تقويم وحدة البصمة** لتمكين موظف جهاز البصمة من معرفة:
- من لديه دوام اليوم / من ليس لديه دوام
- من مجاز / من لديه استراحة
- من يجب مراقبته على الجهاز المحدد

عبر تقويم شهري وسنوي مع فلاتر وتفاصيل اليوم.

---

## المصادر الحالية (بدون تغيير قاعدة البيانات)
- **Device** + **EmployeeFingerprint**: الموظفون المرتبطون بكل جهاز
- **WorkSchedule**: جدول الدوام (شهر/سنة، أيام الأسبوع أو نمط 1x1/1x2/1x3، أوقات البدء/الانتهاء، استراحة breakStart/breakEnd)
- **LeaveRequest** (status=APPROVED): إجازة في تاريخ معيّن
- **Absence**: غياب مسجّل لموظف في تاريخ
- **Holiday**: عطلة رسمية
- **Employee**: departmentId, workType

---

## منطق الحالة ليوم معيّن (لموظف مع جدول)
1. **مجاز**: إجازة معتمدة تغطي هذا التاريخ.
2. **لا دوام**: لا جدول لهذا الشهر، أو اليوم ليس يوم عمل حسب الجدول، أو عطلة رسمية (حسب النطاق).
3. **لم يحضر**: لديه دوام في هذا اليوم لكن وُجد سجل غياب (Absence) لهذا التاريخ.
4. **دوام**: لديه دوام ولا إجازة ولا غياب.
5. **استراحة**: ضمن فترة الاستراحة (breakStart–breakEnd) من الجدول — يُحسب عند الطلب إذا أُعطي وقت (مثلاً "الآن") أو نعرض وقت الاستراحة في تفاصيل اليوم.

---

## الـ API (Backend)

### GET /api/fingerprint-calendar/month
**Query:** `year`, `month`, `deviceId?`, `departmentId?`, `employeeId?`, `workType?`

**Response:** لكل يوم في الشهر:
```json
{
  "year": 2025,
  "month": 2,
  "deviceId": "xxx",
  "days": [
    {
      "date": "2025-02-01",
      "onDuty": 5,
      "onLeave": 2,
      "noSchedule": 1,
      "absent": 0
    }
  ]
}
```
- الموظفون المعنيون: إما كل من مرتبط بالجهاز `deviceId`، أو بعد تطبيق فلتر القسم/الموظف/نوع الدوام.
- الأعداد تُحسب من الحالة الفعلية لكل موظف في ذلك اليوم.

### GET /api/fingerprint-calendar/day
**Query:** `date=YYYY-MM-DD`, `deviceId`, `departmentId?`, `employeeId?`

**Response:**
```json
{
  "date": "2025-02-18",
  "device": { "id", "name", "code", "location" },
  "employees": [
    {
      "employeeId", "fullName", "jobTitle", "departmentName",
      "deviceName", "fingerprintId",
      "startTime", "endTime", "breakStart", "breakEnd",
      "status": "ON_DUTY" | "ON_LEAVE" | "NO_SCHEDULE" | "ABSENT" | "BREAK"
    }
  ]
}
```

---

## الواجهة (Frontend)

### الفلاتر (أعلى الصفحة)
- جهاز البصمة (اسم / موقع / معرّف)
- القسم، اسم الموظف، نوع الدوام، الحالة (دوام–مجاز–استراحة–غياب)
- مترابطة: عند اختيار جهاز → جلب الموظفين المرتبطين به فقط.

### التقويم
- **عرض شهري** (افتراضي): شبكة أيام الشهر، كل يوم قابل للنقر.
- إمكانية التبديل إلى **عرض سنوي** أو التنقل بين الأشهر/السنة.
- داخل كل يوم: أعداد صغيرة ملونة (دوام، مجاز، لا دوام، غياب).

### عند الضغط على يوم
- فتح **صفحة/درج تفصيل اليوم**:
  - قائمة الموظفين المرتبطين بالجهاز المحدد (بعد الفلاتر).
  - لكل موظف: الاسم، الجهاز، وقت بدء/انتهاء الدوام، وقت الاستراحة، **الحالة** (دوام ✅ / استراحة 🟡 / مجاز 🔵 / لا دوام 🔴 / لم يحضر ⚠).

### الأداء
- استعلامات مجمّعة للشهر (بدون تحميل يوم بيوم).
- التخزين المؤقت للشهر الحالي واليوم المفتوح.

---

## الصلاحيات
- نفس صلاحيات **أجهزة البصمة**: FINGERPRINT_OFFICER, FINGERPRINT_MANAGER, DEPARTMENTS_MANAGE, ADMIN.
- الصفحة للعرض الرقابي فقط (لا تعديل).
